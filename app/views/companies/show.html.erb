<h1><%=@company %></h1>

<table>
    <tbody>
        <tr>
            <th>Project</th>
            <th>Company</th>
        </tr>
       <%= render partial: 'projects/project', collection: @works.projects %>
    </tbody>
</table>

<% content_for :aside do %>
 <% render 'sidebar' %>
<% end %>
